<UserControl x:Class="MHW.Wpf.Views.WeaponView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MHW.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type viewModels:WeaponViewModel}}">
  <Grid>
    <StackPanel>
      <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
      <TextBlock Text="{Binding WeaponType}" />
      <TextBlock Text="{Binding Attack}" />
      <TextBlock Text="{Binding Affinity}" />
      <TextBlock Text="{Binding Defense}" />
      <ContentPresenter Content="{Binding Sharpness}"/>
      <!-- Add more details as needed -->
    </StackPanel>
  </Grid>
</UserControl>
