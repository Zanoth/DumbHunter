<UserControl x:Class="Wpf.Design.Controls.SharpnessControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Wpf.Design.Converters"
             xmlns:stats="clr-namespace:SharedDataModels.Abstractions.Gear.Weapons.Stats;assembly=SharedDataModels.Abstractions"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type stats:Sharpness}}">
  <ItemsControl ItemsSource="{Binding Sections}"
                Background="Black"
                Padding="1">
    <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
        <StackPanel Orientation="Horizontal" />
      </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate>
      <DataTemplate>
        <Border Width="{Binding Value}"
                Height="10"
                Background="{Binding Color, Converter={converters:ColorToBrushConverter}}">
        </Border>
      </DataTemplate>
    </ItemsControl.ItemTemplate>
  </ItemsControl>
</UserControl>
